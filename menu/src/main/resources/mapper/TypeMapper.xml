<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lllfff.mapper.TypeMapper">
<select id="findById" parameterType="long" resultType="com.lllfff.entity.Type">
    select * from t_type where id=#{id}
</select>
    <select id="findAll" resultType="com.lllfff.entity.Type">
        select * from t_type
    </select>
</mapper>